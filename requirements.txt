# Verification notebook (vdw_records_notebook.ipynb) and code/ scripts.
# Install with:  pip install -r requirements.txt
numpy>=1.20
jupyter>=1.0          # provides notebook, nbconvert, ipykernel

# Optional: only the standalone SAT experiment in code/sat_full.py needs this.
# python-sat>=1.8
